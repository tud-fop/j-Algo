/* Summation */
#include <stdio.h>

int main()
{
    int i, n, s;

    scanf("%i", &n);

    i = 1;
    s = 0;

    while (i <= n)
    {
        s = s + i * i;
        i = i + 1;
    }

    printf("%d", s);
    return 0;
}

